<div class="container">
  <h4><%= @customer.last_name + @customer.first_name %>さんの注文履歴</h4>
  <div class="row">
    <table>
      <thead>
        <tr>
          <th>購入日時</th>
          <th>注文個数</th>
          <th>注文ステータス</th>
        </tr>
      </thead>
      <tbody>
        <% @tests.each do |orderlist| %>
          <tr>
            <td><%= orderlist.created_at %></td>
            <td><%= orderlist.price %></td>
            <td></td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</diva